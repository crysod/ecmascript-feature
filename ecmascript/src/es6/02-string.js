let hello = 'Hello';
let world = 'World';
let epicPhrase = hello + ' ' + world;
//console.log(epicPhrase);

//Template literals
let epicPhrase2 = `${hello} ${world}, cristian va por tí`;
console.log(epicPhrase2);

// Multi-line string

let lorem = 'esto  es un string \n' + 'Esto es otra linea';

let lorem2 = `esto es un string
otro string`

console.log(lorem2)